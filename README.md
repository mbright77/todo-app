# Todo App

Local-only, offline-first todo app built with React, TypeScript, and IndexedDB.

## Setup

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm run lint`
```
